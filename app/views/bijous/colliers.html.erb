<div class="container">
  <h1>All the Colliers</h1>
  <div class="row">
    <% @bijous.each do |bijou| %>
      <div class="col-6 col-md-4">
        <div class="card">
          <div class="card-body">
            <h5 class="card-title"><%= bijou.name %></h5>
            <%= link_to "More details", bijou_path(bijou), class: "btn btn-primary" %>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
